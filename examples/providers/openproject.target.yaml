provider: openproject
description: OpenProject target for the minimal payment API contract.
config:
  baseURL: https://openproject.example.com
  workPackageTypeHref: /api/v3/types/1
  notify: false
  members:
    - role: software-engineer
      emails: [engineer@example.com]
      openProjectRole: Member
    - role: quality-lead
      emails: [quality@example.com]
      openProjectRole: Member
    - role: engineering-manager
      emails: [manager@example.com]
      openProjectRole: Project admin
  kanban:
    columns: [New, In progress, Closed]
