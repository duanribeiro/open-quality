specVersion: "0.1"
kind: Workflow
metadata:
  id: standard-release
  name: Standard release
spec:
  stages:
    - business-refinement
    - technical-refinement
    - development
    - merge-request
    - continuous-integration
