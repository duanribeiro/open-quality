specVersion: "0.1"
kind: Stage
metadata:
  id: release-approval
  name: Release approval
spec:
  type: review
  reviewScope: release
  dependsOn:
    - deploy-staging
  owner: engineering-manager
  approvalPolicy: production-release-approval
